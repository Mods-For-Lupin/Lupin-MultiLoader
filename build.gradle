plugins {
    id 'fabric-loom' version '1.10-SNAPSHOT' apply(false)
    id 'net.neoforged.moddev.legacyforge' version '2.0.116' apply(false)

    // Forgix, see https://github.com/PacifistMC/Forgix/releases for new versions
    id "io.github.pacifistmc.forgix" version "1.3.3"
}